# Security

Do not commit real BigQuery project exports, dataset IDs, query text, customer data, billing exports, PII samples, access tokens, credentials, screenshots, or production query evidence.

Use synthetic metric metadata only for public portfolio proof.
